// hw_ir.c
#include "hw_ir.h"
#include "stm8s.h"

void hw_ir_init(void) {
    // configurar pino de entrada + EXTI
}

int ir_start(void) {
    // exemplo: retorna 1 se código de "start" foi detectado
    return 0;
}
